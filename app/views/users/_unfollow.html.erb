<%= form_for(current_user.relationships.find_by_followed_id(@user), html: { method: :delete}, remote: true ) do |f| %> 

<!-- We still need to send the follower_id and we can do this throught the hidden field. Needs to be in a div to be valid html. -->
<%=  f.submit "Unfollow", class: "btn btn-large" %>

<% end %>