<% provide(:title, "All users") %>
<h1>All users</h1>


<%= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<%= render 'user', user: user %>
		<!-- Same as  -->
		<!-- <%= render user %> -->
	<% end %>

	<!-- We could even do it this way -->
	<!-- <%= render @users %> -->
</ul>

<%= will_paginate %>